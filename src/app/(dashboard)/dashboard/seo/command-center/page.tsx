import { ComingSoon } from "@/components/layout/ComingSoon";

export default function SeoCommandCenterPage() {
  return (
    <ComingSoon
      title="SEO Command Center"
      description="A single prioritized action list pulling from every SEO tool's history — audits, keyword gaps, GEO mentions — ranked by estimated impact. Buildable now on top of data already being collected across the other tools. Not built yet."
    />
  );
}
