import { ComingSoon } from "@/components/layout/ComingSoon";

export default function ContentStudioPage() {
  return (
    <ComingSoon
      title="AI Content Studio"
      description="Content briefs, full articles, FAQ sections, meta descriptions, and multilingual content — all buildable now with OpenAI, no paid data API required. Not built yet in this pass; ask and it's next."
    />
  );
}
