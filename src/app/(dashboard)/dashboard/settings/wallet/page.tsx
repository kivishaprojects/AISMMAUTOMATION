import { ComingSoon } from "@/components/layout/ComingSoon";

export default function WalletPage() {
  return (
    <ComingSoon
      title="Wallet"
      description="Prepaid credits for AI generation usage — needs a payments provider and a credits ledger table."
    />
  );
}
