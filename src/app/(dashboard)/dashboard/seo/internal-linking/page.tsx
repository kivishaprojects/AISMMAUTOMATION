import { ComingSoon } from "@/components/layout/ComingSoon";

export default function InternalLinkingPage() {
  return (
    <ComingSoon
      title="Internal Linking Engine"
      description="Crawl a handful of your pages, cluster them by topic, and suggest which pages should link to each other. Buildable on top of the same crawler used in the Technical SEO Auditor — scoped to pages you provide rather than a full-site crawl. Not built yet."
    />
  );
}
