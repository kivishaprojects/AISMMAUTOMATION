import { ComingSoon } from "@/components/layout/ComingSoon";

export default function SeoAutomationPage() {
  return (
    <ComingSoon
      title="SEO Automation Workflows"
      description="Sitemap submission pings and content-refresh alerts (flagging audits past a staleness threshold) are genuinely buildable without new accounts. CMS auto-publish depends on which CMS your sites use. Not built yet — ask and I'll start with sitemap pings + stale-content alerts."
    />
  );
}
